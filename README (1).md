# Women Techmakers Knowledge Hub

Status: **Pilot, internal review.** Not yet shared with Ambassadors.

This repo holds the pared-down v1 of the WTM Knowledge Hub: a single static
`index.html` file with no build step and no external dependencies, so it
works fully offline and loads fast on a slow connection.

## Viewing it

Once GitHub Pages is turned on for this repo (Settings → Pages → Source:
`main` branch, `/ (root)` folder), the live page is at:

`https://<org-or-username>.github.io/<repo-name>/`

## Editing it

There's no build process. Edit `index.html` directly, commit, and push to
`main`. GitHub Pages rebuilds automatically, usually within a minute or two.

Brand colors, spacing, and type roles are defined as CSS custom properties
at the top of the `<style>` block, so a palette or type change happens in
one place.

## Content owner

Naomi Latini Wolfe (naomi@technovation.org), Community Development
Specialist. Program owner: Raeana Samuel (wtm@technovation.org).

## Open items

Several resources on the page are marked "Coming soon" rather than linked,
because the underlying program (Trained Regional Facilitator pathway,
Oxford AI Leadership Certificate, Women Developer Academy, AI Ventures
Accelerator) or asset (event run-of-show template, caregiver-friendly event
guide, speaker briefing guide) isn't finalized yet. Update those links as
each becomes available.
